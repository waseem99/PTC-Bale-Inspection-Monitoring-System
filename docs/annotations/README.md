# Restricted action annotations

Repository files in this directory define annotation semantics and empty templates only.

Actual factory footage, extracted frames, completed annotations, datasets and trained model binaries remain in approved protected storage and must not be committed to GitHub.

Current target action schema: `action-labels-v2.md`.
